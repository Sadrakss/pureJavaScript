var nome = 'joão', apelido = 'pedro', pais = portugal, idade = 12
var idade = 10


var animal
animal = 'lion'

var play = true
function teste() {
    var stop = false

    // arrays
    var nomes = [
        'sadrak', 'pedro', 'maria'
    ]

    // objects
    var car = {
        brand: 'Mercedes',
        year: 2019,
        color: 'grey'
    }

    document.getElementById('info').innerHTML = car.brand + ' ( ' + car.year + ' ) '
}
